.venv/
.pytest_cache/
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.mypy_cache/
.ruff_cache/
*.db
*.db-journal
*.db-wal
*.db-shm

# Claude Code dev-server config — machine-specific absolute paths.
# A portable shared version can be added later if we want one.
.claude/

# Local config contains secrets (DB path, Cognito credentials).
# Copy config.yaml.example → config.yaml and fill in your values.
config.yaml

# Scratch area for outside repos staged during dev — never commit.
_external/
